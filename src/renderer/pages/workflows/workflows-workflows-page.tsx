import { observer } from "mobx-react";

/**
 * Placeholder for Phase 3: Workflow list and actions.
 */
export const ArgoWorkflowsWorkflowsPage = observer(() => (
  <div style={{ padding: "1.5rem" }}>
    <h2 style={{ marginTop: 0 }}>Workflows</h2>
    <p>Argo Workflows resource listing and menus will be implemented in a future release.</p>
  </div>
));
