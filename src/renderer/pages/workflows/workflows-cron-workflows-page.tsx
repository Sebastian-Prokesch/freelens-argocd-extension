import { observer } from "mobx-react";

/**
 * Placeholder for Phase 3: CronWorkflow list and actions.
 */
export const ArgoWorkflowsCronWorkflowsPage = observer(() => (
  <div style={{ padding: "1.5rem" }}>
    <h2 style={{ marginTop: 0 }}>CronWorkflows</h2>
    <p>Argo Workflows CronWorkflow listing will be implemented in a future release.</p>
  </div>
));
