import { observer } from "mobx-react";

/**
 * Placeholder for Phase 3: Rollout list and promotion actions.
 */
export const ArgoRolloutsRolloutsPage = observer(() => (
  <div style={{ padding: "1.5rem" }}>
    <h2 style={{ marginTop: 0 }}>Rollouts</h2>
    <p>Argo Rollouts resource listing and menus will be implemented in a future release.</p>
  </div>
));
